遇到最短路题时，我会先问两个问题：边权是否相同？状态是否只由当前位置决定？

### 边权相同：BFS

当每条边的代价都相同，BFS 第一次访问到某个状态时就已经得到最短距离。它依赖队列的层序性质。

### 边权非负：Dijkstra

当边权不同但非负，用优先队列维护当前最小距离。核心判断是：如果弹出的距离已经不是最新距离，就跳过。

```js
if (distance !== dist[node]) continue;
```

真正需要小心的是状态设计。比如带钥匙的迷宫，节点不能只是坐标 `(x, y)`，还要包含钥匙集合 `mask`。
